<UserControl
    x:Class="LoquatDocs.Pages.DocumentListItem"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="{Binding RelativeSource={RelativeSource Self}}">

  <StackPanel>
    <TextBlock Visibility="{Binding IsGroup}" Text="{Binding Title}"/>
    <Grid Visibility="{Binding IsDocument}" BorderBrush="Black" BorderThickness="1">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>
      <TextBlock Grid.Column="0" Text="{Binding Title}"/>
      <TextBlock Grid.Column="1" Text="{Binding DateAsString}"/>
      <TextBlock Grid.Column="2" Text="{Binding PayedText}" Visibility="{Binding IsInvoice}"/>
      <Button Grid.Column="3" Click="OpenDocument">
        <SymbolIcon Symbol="OpenFile" />
      </Button>
    </Grid>

  </StackPanel>
</UserControl>
